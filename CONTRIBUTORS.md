# Contributors

本文件用于记录项目协作者与贡献方向。GitHub 页面里的 `Contributor` 不是手动角色，而是由进入默认分支的 commit 自动统计。

## Team

| GitHub 用户 | 项目角色 | 贡献方向 | 状态 |
| --- | --- | --- | --- |
| `wenhaozhou07-gif` | Owner | 产品架构、前后端实现、DeepSeek 翻译链路、README 与 Demo 材料 | 已提交 |
| `XYL2120211566` | Collaborator | Demo 验证、README 校对、功能体验反馈 | 待本人提交确认 |

## 给 `XYL2120211566` 的第一次贡献任务

请用自己的电脑和 GitHub 账号完成下面任意一个小改动，然后提交并推送：

1. 把上表中自己的状态从 `待本人提交确认` 改成 `已提交`。
2. 在 `贡献方向` 中补充你实际参与的工作。
3. 在 README 或 demo 脚本中修正一个表达、错别字或演示说明。

提交示例：

```bash
git clone https://github.com/wenhaozhou07-gif/-.git
cd -
git config user.name "XYL2120211566"
git config user.email "你的 GitHub 绑定邮箱"
git add CONTRIBUTORS.md README.md docs/demo-script.md
git commit -m "Add collaborator contribution"
git push origin main
```

推送成功后，GitHub 会自动把该账号显示为项目 Contributor。刷新可能需要几分钟。
